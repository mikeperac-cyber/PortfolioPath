import { NextResponse } from "next/server"
import { z } from "zod"
import { getApiContext, invalid } from "@/lib/api-auth"
import { canCreateProject, type Entitlements } from "@/lib/entitlements"
import { findUnsupportedClaims, sanitizePlainText } from "@/lib/security"
const schema=z.object({title:z.string().trim().min(3).max(160),category:z.string().min(2).max(100),motivation:z.string().min(10).max(3000),problem:z.string().min(10).max(3000),objective:z.string().min(10).max(1500),audience:z.string().min(2).max(1000),outcome1:z.string().min(3).max(500),outcome2:z.string().min(3).max(500),outcome3:z.string().min(3).max(500),startDate:z.iso.date(),endDate:z.iso.date(),weeklyHours:z.coerce.number().min(1).max(30),milestones:z.string().min(5).max(5000),evidencePlan:z.string().min(5).max(3000),skills:z.string().min(2).max(1000),risks:z.string().min(5).max(3000),deliverable:z.string().min(3).max(1000)})
export async function POST(request:Request){try{const input=schema.parse(await request.json());const context=await getApiContext(["student"]);if(context.error)return context.error;const {supabase,user}=context;const combined=Object.values(input).join(" ");const claims=findUnsupportedClaims(combined);if(claims.length)return NextResponse.json({error:`Qualify unsupported wording before submission: ${claims.join(", ")}`},{status:422});const {count}=await supabase.from("projects").select("id",{count:"exact",head:true}).eq("student_id",user.id).neq("status","archived");const {data:subscription}=await supabase.from("subscriptions").select("plans(entitlements)").eq("user_id",user.id).eq("status","active").maybeSingle();const entitlements=((subscription?.plans as unknown as {entitlements:Entitlements}|null)?.entitlements??{project_limit:1});if(!canCreateProject(entitlements,count??0))return NextResponse.json({error:"Your current plan project limit has been reached."},{status:403});const {data:category}=await supabase.from("project_categories").select("id").eq("name_en",input.category).maybeSingle();const {data:project,error}=await supabase.from("projects").insert({student_id:user.id,category_id:category?.id??null,title:sanitizePlainText(input.title),personal_motivation:sanitizePlainText(input.motivation),problem_opportunity:sanitizePlainText(input.problem),main_objective:sanitizePlainText(input.objective),target_audience:sanitizePlainText(input.audience),start_date:input.startDate,end_date:input.endDate,weekly_hours:input.weeklyHours,risks:sanitizePlainText(input.risks),final_deliverable:sanitizePlainText(input.deliverable),status:"awaiting_counselor_review"}).select("id").single();if(error)throw error;await supabase.from("project_outcomes").insert([input.outcome1,input.outcome2,input.outcome3].map((description,index)=>({project_id:project.id,description:sanitizePlainText(description),target_value:null,unit:null,sort_order:index})));const milestones=input.milestones.split(/\r?\n/).map(v=>v.trim()).filter(Boolean);await supabase.from("project_weeks").insert(milestones.map((milestone,index)=>({project_id:project.id,week_number:index+1,milestone:sanitizePlainText(milestone)})));return NextResponse.json({id:project.id,status:"awaiting_counselor_review"},{status:201})}catch(error){return invalid(error)}}
